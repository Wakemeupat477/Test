{
  "name": "mail-threads-task-solution",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.16.0"
  },
  "scripts": {
    "worker": "node --experimental-strip-types src/worker.mts",
    "export": "node --experimental-strip-types src/exporter.mts",
    "test": "node --experimental-strip-types --test tests/*.test.mts",
    "check": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.8.3"
  }
}
